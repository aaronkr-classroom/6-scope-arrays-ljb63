#include<stdio.h>

int sum(int x, int y){
    int result = x + y; 
    return restult;
}

int main(void){
    int a = 3, b = 5;
    result = sum(a, b);
    printf("%d + %d + %d", a, b ,result);

    return 0;
}